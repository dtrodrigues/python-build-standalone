# Describes the targets that the CI system will build and test on.
linux:
  x86_64-unknown-linux-gnu:
    arch: x86_64
    libc: gnu
    python_versions:
      - "3.9"
      - "3.14"
    build_options:
      - debug
    run: true

